generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

//Designing user table
model User{
  id Int @id @default(autoincrement())
  name String
  lastname String?
  email String @unique
  password String

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}